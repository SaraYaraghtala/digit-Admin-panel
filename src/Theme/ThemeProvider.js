import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#EE384E",
      light: "#F8EBED",
      dark: "#AB717D",
      contrastText: "#fff",
    },
  },
});
export default theme;
